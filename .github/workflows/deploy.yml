name: Build and Deploy Documentation

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python with pip cache
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'  # Кэширование Python пакетов

      - name: 📦 Cache apt packages
        uses: actions/cache@v4
        with:
          path: |
            /var/cache/apt
            /var/lib/apt/lists
          key: ${{ runner.os }}-apt-${{ hashFiles('.github/apt-packages.txt') }}
          restore-keys: |
            ${{ runner.os }}-apt-

      - name: 📦 Install system dependencies
        run: |
          # Быстрая проверка установлен ли LibreOffice
          if ! command -v libreoffice &> /dev/null; then
            echo "Installing LibreOffice..."
            sudo apt-get update
            sudo apt-get install -y libreoffice
          else
            echo "✅ LibreOffice already installed"
            libreoffice --version | head -1
          fi

      - name: 📋 Install Python packages
        run: |
          pip install --upgrade pip
          pip install PyYAML Jinja2 markupsafe python-docx odfpy click colorama

      - name: 🏗️ Generate documents
        run: |
          echo "🏗️ Generating GOST documents..."
          python docs/scripts/builders/build_docs.py all
          echo "✅ Documents generated in docs/output/pdf/"

      - name: 📄 Copy PDFs to web folder
        run: |
          echo "📄 Preparing PDFs for website..."
          
          # Создаем папку для PDF
          mkdir -p docs/output/web/pdf
          
          # Копируем PDF файлы
          if [ -d "docs/output/pdf" ] && [ "$(ls -A docs/output/pdf/*.pdf 2>/dev/null)" ]; then
            echo "Found PDF files:"
            ls -la docs/output/pdf/*.pdf
            
            # Копируем
            cp docs/output/pdf/*.pdf docs/output/web/pdf/
            
            echo "✅ PDFs ready in web/pdf/:"
            ls -la docs/output/web/pdf/*.pdf
          else
            echo "⚠️ No PDFs to copy"
            # Пустой файл чтобы папка существовала
            touch docs/output/web/pdf/.keep
          fi

      - name: 🌐 Generate website
        run: |
          echo "🌐 Generating website..."
          python docs/scripts/builders/build_site.py
          
          # Обязательно для GitHub Pages
          touch docs/output/web/.nojekyll
          
          echo "✅ Website generated"

      - name: 📤 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs/output/web
          publish_branch: gh-pages
          force_orphan: false
          enable_jekyll: false